{
  "annotations": {"list": []},
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "graph",
      "title": "HTTP Requests (user-service)",
      "targets": [{
        "expr": "sum(rate(http_server_requests_seconds_count{job='user-service'}[1m]))",
        "legendFormat": "{{method}} {{uri}} {{status}}"
      }],
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 0}
    },
    {
      "type": "graph",
      "title": "HTTP Requests (monitoring-service)",
      "targets": [{
        "expr": "sum(rate(http_server_requests_seconds_count{job='monitoring-service'}[1m]))",
        "legendFormat": "{{method}} {{uri}} {{status}}"
      }],
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8}
    },
    {
      "type": "stat",
      "title": "Users Created",
      "targets": [{
        "expr": "sum(user_created_total)"
      }],
      "gridPos": {"h": 4, "w": 12, "x": 0, "y": 16}
    },
    {
      "type": "stat",
      "title": "Users Deleted",
      "targets": [{
        "expr": "sum(user_deleted_total)"
      }],
      "gridPos": {"h": 4, "w": 12, "x": 12, "y": 16}
    }
  ],
  "schemaVersion": 27,
  "style": "dark",
  "tags": ["demo"],
  "templating": {"list": []},
  "time": {"from": "now-15m", "to": "now"},
  "timezone": "",
  "title": "E2E Monitoring Demo",
  "uid": "e2e-demo-dashboard",
  "version": 1
}
